# SSL fix for macOS Python 3.12+ (uncomment if you get SSL cert errors)
# Run: python -c "import certifi; print(certifi.where())" to get the path
# SSL_CERT_FILE=/path/to/certifi/cacert.pem

# Binance API (optional for Phase 1 — public market data works without keys)
BINANCE_API_KEY=
BINANCE_API_SECRET=

# Anthropic Claude API (needed in Phase 4 for sentiment analysis)
ANTHROPIC_API_KEY=

# CryptoPanic API (needed in Phase 4 for news headlines)
CRYPTOPANIC_API_KEY=
